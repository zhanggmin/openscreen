- 所有 BrowserWindow 实例在创建时均配置 `preload.mjs` 脚本并启用 `contextIsolation: true` 以确保渲染进程安全。
- 窗口加载逻辑统一适配开发与生产环境，根据 `VITE_DEV_SERVER_URL` 存在与否动态选择 `loadURL` 或 `loadFile`。
- 采用防御性编程模式，在执行窗口操作（如移动、调整大小、发送消息）前强制检查窗口实例是否存在且未被销毁（`!isDestroyed()`）。