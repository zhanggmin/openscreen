- **Electron IPC 架构**：采用 `contextIsolation: true` 与 `contextBridge` 确保渲染进程安全，通过 `ipcMain.handle/invoke` 实现异步双向通信。
- **原生进程集成**：在 Windows/macOS 上通过 `node:child_process` 调用外部二进制助手（如 `wgc-capture.exe` 或 `openscreen-screencapturekit-helper`）以实现高性能屏幕捕获。