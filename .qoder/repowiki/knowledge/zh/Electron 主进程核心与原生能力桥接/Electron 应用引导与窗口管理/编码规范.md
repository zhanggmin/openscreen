- 所有窗口创建函数均通过 `additionalArguments` 注入 `--asset-base-url`，以解决沙箱环境下预加载脚本无法访问 Node.js 路径模块的问题。
- IPC 处理程序普遍采用统一的响应结构 `{ success: boolean; error?: string; data?: T }`，并在 `handlers.ts` 中通过 try-catch 包裹以确保主进程稳定性。
- 窗口实例在关闭时统一置为 `null` 并配合 `isDestroyed()` 检查，防止对已销毁的 BrowserWindow 实例进行操作引发崩溃。