- 所有跨进程通信必须通过 `src/native/contracts.ts` 定义的契约接口进行，禁止直接访问 Electron 内部 API。
- UI 组件统一采用 `shadcn/ui` 风格并结合 `tailwindcss` 进行样式编排，确保视觉一致性。
- 原生模块适配层采用工厂模式（Factory Pattern）根据操作系统动态实例化对应的录制会话（如 `macNativeCursorRecordingSession`）。