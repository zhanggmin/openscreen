- **分层结构**：文档按功能域划分为入门指南、架构设计（IPC/窗口管理）、录制系统、视频编辑器、导出管线及构建工程。
- **核心接口定义**：通过 `02-architecture/01-ipc-communication-system.md` 详细定义了主进程与渲染进程间的 32 个 IPC 接口契约及安全隔离机制。
- **子系统边界**：明确划分了录制（MediaRecorder/WebM）、编辑（Pixi.js/React 状态管理）与导出（WebCodecs/Canvas 渲染）三大核心子系统的职责与数据流转路径。
- **入口索引**：以 `README.md` 和 `01-overview.md` 为全局入口，建立从高层概览到具体实现细节（如光标遥测、时间轴拖拽）的导航体系。