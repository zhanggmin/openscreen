- **开发启动**：执行 `npm run dev` 启动 Vite 开发服务器并自动拉起 Electron 主进程，支持渲染层 HMR。
- **平台构建**：使用 `npm run build:mac/win/linux` 调用 electron-builder 生成特定平台的安装包（dmg/nsis/AppImage）。
- **代码规范**：通过 `npm run lint:fix` 和 `npm run format` 调用 Biome 进行统一的代码检查与格式化。